# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
*.whl

# Virtual environments
venv/
venv-build/
.venv/
env/

# PyInstaller
*.spec.bak
wright-telemetry.pkg
*.dmg

# PyUpdater build artifacts and signing keys (never commit pyu-data/)
pyu-data/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Wright Telemetry local config (contains credentials)
config.json
!tests/fixtures/**/config.json
*.log

# Environment variables / secrets
.env
.env.*

# Testing
.pytest_cache/
htmlcov/
.coverage

# Raw miner API dumps (scripts/*_api_dump.ps1) — contain real MACs, IPs,
# pool worker names and session tokens. Anonymise before adding as a fixture.
*_dump.txt
vnish_dump*.txt
